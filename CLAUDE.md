# CLAUDE.md (Project Root)

저장소 전체에 적용되는 **항구적 지침**이다. 프로젝트 성격, 디렉터리 구조, spec과의 관계, 비밀·개인 데이터 관리, 작업 흐름 등 언어·모듈과 무관한 토대를 다룬다.

언어·모듈에 특화된 규칙(백엔드 Java/Spring의 계층·파일 I/O 경계·테스트)은 해당 하위 디렉터리의 `CLAUDE.md`에 있다. 그 디렉터리에서 작업할 때는 루트 + 해당 모듈 `CLAUDE.md`를 함께 따른다.

---

## 1. 프로젝트 성격

**모카(Mocha)** — **앱(PWA)에** 자연어로 던진 커피 감상을 구조화해 로컬 DB에 기록하고, 갤러리·상세로 다시 보며, 공유할 때 시음일 카드 이미지로 굽는 1인용 개인 에이전트. *(입구가 Slack에서 앱으로 바뀐 것은 changes/0029 — 캡처는 여전히 자연어이고 바뀐 것은 확인·수정·조회의 매체다.)* 개인 실사용이 1차 목적이고, 실사용하며 델타로 개선한 기록 자체가 포트폴리오 자산이다. 일반 사용자 릴리즈는 미래 옵션(spec 비범위)이며, 이 문서는 그와 무관하게 유지되는 토대만 규정한다.

## 2. 저장소 구조

- `specs/` — 기능별 명세(SDD). **모든 기능 요구사항·설계 결정의 source of truth.** 현재 유일 기능: `specs/coffee-note-agent/`.
- 백엔드(Java/Spring) — 소스 트리. 해당 디렉터리의 `CLAUDE.md`가 구현 규칙을 규정한다.
- **DB(PostgreSQL)** — **원본 데이터의 소유자**(ADR-73). 노트·시음일·회차·**사진 색인(`note_photo`)**이 행으로 산다(구 `pending`은 changes/0029에서 폐기 — 작성 중인 폼은 클라이언트가 소유한다). 레포 트리 밖에 있고(로컬은 `docker-compose.yml`의 컨테이너 볼륨), 트리에 있는 것은 **스키마 정의뿐**이다 — Flyway 마이그레이션(`src/main/resources/db/migration/`)이 스키마를 단독 소유한다.
- `data/` — **런타임 산출(파일로 남는 원본)**: 사진 아카이브(`photos/`)와 업로드 스테이징. 노트·pending은 여기 없다(전자는 0028에서 DB로, 후자는 0029에서 폐기). 사진 바이트가 정본이고 `note_photo` 행은 그 색인이다(ADR-79). 커밋하지 않는다(§5).
- `artifact/` — **런타임 산출(파생물)**: 시음일 카드 JPG(`cards/`). index HTML은 ADR-55로, 썸네일은 ADR-32로 폐기됐다. **DB에서 전체 재생성 가능**하므로 커밋하지 않는다.
- `frontend/` — **클라이언트 소스 트리**(React + TypeScript + Vite). 앱 UI가 사는 곳이고 백엔드와 **한 저장소·한 프로세스**로 돈다: `./gradlew build`가 프론트를 함께 빌드하고 산출물을 Spring이 정적 서빙한다(changes/0029 ADR-78). 산출물은 `build/frontend/`로 나가므로 이 트리에는 소스만 있다. `node_modules/`는 커밋하지 않고 `package-lock.json`이 정본이다.
- (이후 모듈이 추가되면 각 모듈 루트에 `CLAUDE.md`를 둔다.)

## 3. spec과의 관계 (SDD)

- 각 기능은 `specs/<feature>/`에 spec(요구사항)·plan(설계·ADR)·data-model·tasks를 가진다.
- **기능을 구현·수정하기 전에 반드시 해당 spec을 정독한다.** 충돌 시 spec을 우선한다.
- 구현 순서: spec 정독 → tasks의 Phase 순 → 각 작업이 어느 FR/AC/ADR을 충족하는지 의식하며 구현.
- **spec에 없는 동작을 임의로 추가하지 않는다.** 필요하다고 판단되면 spec에 먼저 반영(또는 사람에게 제안)한 뒤 구현한다.
- 설계 결정을 바꿔야 하면 코드만 바꾸지 말고 해당 ADR/spec도 갱신한다. 코드와 문서가 어긋나지 않게 유지한다. 기존 기능 수정은 델타(`changes/<NNNN>-<slug>/`)로 남긴다.

## 4. 공통 원칙

- **right-sizing**: 미래 대비 선제 도입을 하지 않는다. 새 인프라(DB, 캐시, 메시지 브로커, 웹서버 서빙 등)나 무거운 추상화는 *문제가 실제로 관측됐을 때* 도입한다. 필요해 보이면 코드로 들어가기 전에 사람에게 묻는다. 미래 옵션(호스팅형, jpackage 배포 등)을 위한 선제 일반화도 금지 — spec이 요구한 인터페이스 경계(NFR-4)까지만.
- **실사용 우선순위**: 기록이 실제로 쌓이는 것이 최우선 가치다. 화려함보다 "메시지 한 줄 → 저장까지"의 마찰 최소화. 개선은 실사용에서 관측된 불편에서 출발한다.
- **문서-코드 일치**: 결정과 구현이 갈라지지 않게, 둘 중 하나를 바꾸면 다른 하나도 맞춘다.

## 5. 비밀 / 개인 데이터

- 비밀: OpenAI API 키(구 Slack bot·app token은 changes/0029에서 소멸). 코드·설정·커밋에 하드코딩 금지. 프로젝트 루트 `.env` → 환경변수로 주입.
- **`.env`는 절대 커밋 금지.** `.gitignore`에 포함. 형식 공유는 `.env.example`(빈 값)로.
- OpenAI 키는 종량제 API 키이며, 일반 구독과 별개임을 인지한다.
- **개인 데이터도 비밀에 준해 다룬다.** `data/`(사진)와 **DB의 노트 데이터**에는 개인 일상 정보가 담기므로 커밋·공개 금지, `.gitignore`에 포함. 레포를 포트폴리오로 공개할 때는 실데이터가 아니라 별도 샘플 데이터로 시연한다.
  - **DB 덤프·백업 파일을 레포 안에 만들지 않는다** — `data/`가 아니어서 `.gitignore`가 잡아주지 않는다.
  - **로컬 개발 DB 자격증명은 예외**다: `docker-compose.yml`·`application.yaml`의 `mocha`/`mocha`는 외부에 노출되지 않는 로컬 전용이라 평문으로 둔다(docker-compose.yml POLICY). 클라우드 이전(델타 B) 시 `MOCHA_DB_*` 환경변수 주입으로 바꾸고, 그 값부터는 비밀이다.
- **`logs/`도 개인 데이터에 준해 다룬다.** 로그에 사용자 메시지 원문(감상)이 남을 수 있다(spec NFR-7) — 커밋·공개 금지, `.gitignore` 유지.
- **원문은 LLM API 경유로 외부에 나간다.** 발화 원문은 요청 페이로드로 OpenAI에 전송되며, 에이전트 루프는 여기에 더해 서버측 대화 보존(`store` — `previous_response_id` 전제)까지 수용한다. **전송은 어떤 설정으로도 없앨 수 없고**(감상 원문이 곧 처리 대상), `store`가 가르는 것은 대화 객체 보존 여부뿐이다 — `store` 전환을 개인정보 대책으로 오해하지 않는다. 이 트레이드오프는 인지·수용된 결정이며 `specs/coffee-note-agent/plan.md#ADR-71`이 단일 소유한다. 보존 방식을 바꾸려면 코드가 아니라 그 ADR을 먼저 갱신한다.

## 6. 작업 전 체크 (공통)

- 건드리는 기능의 `specs/<feature>/` 문서를 읽었는가.
- 새 동작이 spec 범위 안인가. (범위 밖이면 spec 반영/제안 먼저)
- 추가하려는 인프라/의존성이 §4 right-sizing에 맞는가. (애매하면 묻는다)
- 비밀·개인 데이터(`.env`, `data/`)가 코드/커밋에 들어가지 않는가.
- 작업 디렉터리에 모듈 `CLAUDE.md`가 있으면 그 규칙도 함께 따랐는가.
