# REVIEW.md

코드 리뷰 시 적용하는 기준이다. 모카는 1인용 로컬 에이전트다(루트 `CLAUDE.md` §1, §4 right-sizing). 리뷰의 방향도 같다: **작게 유지하고, 실제로 관측된 필요만 반영한다.** 또한 이 프로젝트는 LLM 에이전트 애플리케이션으로서 **프롬프트 엔지니어링보다 하네스 엔지니어링을 우선한다** — 동작의 정확성은 프롬프트 지시가 아니라 코드 레벨 구조(스키마, 검증, 타입, 루프 제어)로 보장되어야 한다. 애매한 케이스는 이 두 원칙을 방향타로 판단하라. 이 문서와 spec이 충돌하면 spec이 우선한다.

---

## severity 등급

- **🔴 Important** — 머지 전 수정한다. 데이터 오염·무한 루프·비용 폭주·잘못된 저장으로 이어질 수 있는 위반.
- **🟡 Minor** — 권고. 그 PR에서 정리하는 것이 기본이나, 범위가 커지면 백로그로 남겨도 된다.
- 확신이 없는 finding은 한 단계 낮춰 보고하고 불확실함을 명시한다('검증 기준' 참조).

## 1. 의존 대상을 이름에 드러낼 것

구현 클래스는 **무엇에 의존하는지 이름만 보고 알 수 있어야 한다.**

- 외부 서비스·기술에 의존하는 구현체는 그 대상을 접두어로 명명한다.
  - 예: `OpenAiChatClient`, `JpaNoteRepository`, `ThymeleafNoteRenderer`, `PlaywrightCardImageRenderer`, `LocalPhotoStore`
  - 나쁜 예: `ChatClientImpl`, `DefaultNoteRepository`, `NoteRepositoryImpl` — `Impl`/`Default` 접두·접미어는 의존 정보를 숨기므로 금지.
  - **예외: 프레임워크가 이름을 강제하는 자리.** Spring Data의 custom fragment 구현체는 `<FragmentInterface>Impl`이어야 인식된다(`NoteEntityRepositoryCustomImpl`) — 이름을 바꾸면 배선이 끊긴다. 규칙의 목적이 "의존을 숨기지 말 것"인데 여기서는 프레임워크 규약이 곧 의존 정보다. 같은 부류가 아닌 자리에서 `Impl`을 쓰면 그대로 금지다.
- 클래스 내부에서도 외부 의존(HTTP 클라이언트, 파일 시스템, 프로세스 실행 등)은 필드·생성자 시그니처에 드러나야 한다. 메서드 깊숙한 곳에서 몰래 `new`로 외부 자원을 만들지 않는다.
- **표준·프레임워크에 대응 개념이 있으면 그 용어를 따른다.** 우리가 만드는 타입이 Spring AI(또는 Spring, JDK)의 기존 개념과 같은 역할이면 자체 조어를 만들지 말고 대응되는 이름을 쓴다.
  - 예: LLM 대화 호출 경계는 `ChatClient`, 툴 정의 공급은 `ToolCallback`/`ToolCallbackProvider`, 모델 호출은 `ChatModel` — Spring AI 용어와 1:1로 읽히게 한다.
  - 나쁜 예: 같은 역할에 `LlmCaller`, `PromptExecutor`, `FunctionSpecHolder` 같은 자체 조어를 붙이는 것. 프레임워크 문서·스택트레이스와 어휘가 어긋나 대응 관계를 매번 추론해야 한다.
  - 단, 역할이 실제로 다르면 표준 이름을 억지로 빌려 오지 않는다(같은 이름 다른 의미가 더 나쁘다). 우리 도메인 고유 개념(`NoteRepository`, `PhotoBufferStore` 등)은 도메인 어휘를 쓴다.

**리뷰 질문**: 이 클래스 이름만 보고 뭘 쓰는지 알 수 있는가? 숨은 외부 의존이 있는가? 표준(Spring AI 등)에 이미 같은 역할의 이름이 있는데 자체 조어를 쓰고 있지는 않은가?

## 2. 추상화는 교체 가능성이 실재하는 경계에만

인터페이스는 **"실제로 교체하거나 테스트에서 격리할 이유가 있는 경계"**에만 둔다. 현재 허용된 경계:

| 경계 | 인터페이스 | 이유 |
|---|---|---|
| LLM 호출 | `ChatClient`, `VisionClient`, `AliasGenerator` | 모델/프로바이더 교체 가능성, 테스트 격리 |
| LLM 호출 (세그먼터) | `UtteranceSegmenter` | 다중 날짜 발화의 날짜별 분리 전용 전처리(plan ADR-61, changes/0023) — 모델/프로바이더 교체 가능성, 테스트 격리 |
| 저장 | `NoteRepository`, `PendingStore`, `PhotoBufferStore`, `PhotoStore` | 저장 방식 교체 가능성(NFR-4), 테스트 격리 — changes/0028이 파일→DB로 갈아끼우며 실증됐다(상위 계층 테스트 무변경 = AC-Δ1) |
| 메시지 송수신 (Slack) | `SlackResponder`, `PhotoDownloader`, `ConversationRouter` | 외부 서비스 경계, 테스트 격리 |
| 렌더링 | `NoteRenderer`, `CardImageRenderer` | 템플릿/렌더링 엔진 경계 |

이 표 밖의 새 인터페이스는 **기본적으로 반려**한다. 정말 필요하면 이 표에 행을 추가하는 변경(사유 포함)과 함께 온다.

**단, 경계 *안쪽*의 인터페이스는 이 표의 대상이 아니다.** 판별 기준은 개수가 아니라 **"상위 계층이 이 인터페이스를 아는가"**다. `NoteEntityRepository`·`NoteEntityRepositoryCustom`(changes/0028 ADR-73의 저장소 3분할)은 Spring Data가 프록시를 만들기 위해 요구하는 선언이고 `NoteRepository` 구현체 안쪽에만 있다 — 교체 가능성을 위한 추상화가 아니므로 행을 늘리지 않는다. 반대로 상위 계층이 타입을 직접 잡는 순간 그것은 새 경계이고 이 표를 통과해야 한다.

> **`NoteRepository` 행은 A1 시점의 모습이다** — 지금은 저장 계약에 더해 병합 정책(ADR-4·59)·V-9·V-13까지 지고 있다(`service` 패키지 부재). A2(`0029-app-interface`)가 service를 세우며 정책을 옮기고 이 인터페이스를 저장소 계약으로 좁힌다. **그 전까지 "저장소가 정책을 안다"를 finding으로 올리지 않는다** — 이미 결정돼 일정이 잡힌 부채다.

이 표가 허용 경계 목록의 **단일 소유 지점**이다 — 다른 문서(`src/main/java/CLAUDE.md` §2 등)는 이 표를 참조만 하고 인터페이스 이름을 중복 나열하지 않는다(문서 간 드리프트 방지).

- 구현체가 하나뿐이고 앞으로도 하나일 클래스에 인터페이스를 씌우지 않는다.
- "나중에 필요할지도 모르는" 제네릭 파라미터, 전략 패턴, 팩토리, 이벤트 버스를 선제 도입하지 않는다.
- 단순 위임만 하는 래퍼 클래스(한 줄 통과 계층)를 만들지 않는다.

**리뷰 질문**: 이 인터페이스를 지우고 구현 클래스를 직접 쓰면 뭐가 나빠지는가? 답이 "없다"면 지운다.

## 3. 데드 코드는 발견 즉시 제거

- 호출자 없는 public 메서드, 사용처 없는 클래스·필드·상수, 주석 처리된 코드 블록, 도달 불가 분기는 **리뷰에서 발견되면 그 PR에서 지운다.** "나중에 쓸지도 몰라서"는 사유가 아니다 — git 히스토리가 보관한다.
- 미래를 위한 파라미터(항상 같은 값이 넘어오는 인자, 항상 null인 옵션)도 데드 코드다.
- spec에서 제거된 기능의 잔재 코드가 남아 있으면 함께 지운다.

**리뷰 질문**: 이 심볼의 사용처를 IDE로 찾았을 때 프로덕션 호출자가 있는가?

## 4. 아키텍처는 이 규모에 맞게

- **헥사고날·클린 아키텍처식 계층 분리를 도입하지 않는다.** port/adapter, usecase/interactor, application/infrastructure 같은 계층 패키지·명명을 만들지 않는다. §2의 인터페이스 경계면 충분하다.
- **일반 도메인 모델링(엔티티-VO-애그리게잇-도메인서비스 구분, 리치 도메인 모델)을 하지 않는다.** `domain/` 패키지는 노트 구조를 그대로 반영하는 record 중심의 데이터 홀더로 유지한다. 로직은 그 데이터를 쓰는 쪽(agent, render, slack)에 둔다.
- 매퍼 계층·DTO 변환 계층을 습관적으로 추가하지 않는다. 같은 데이터를 표현하는 클래스가 두 벌 생기면 그 자체가 리뷰 지적 대상이다(뷰 전용 가공이 실제로 필요한 `NoteView` 같은 경우만 예외).
  - **JPA 엔티티(`repository/entity/`)와 `NoteEntityMapper`는 확정된 예외**다(changes/0028 TΔ3a·TΔ3c). 도메인 record를 엔티티로 바꾸지 않기로 한 결과이고 — 그랬다면 영속 관심사(`@Entity`·getter·기본 생성자·가변성)가 `domain/`으로 새고 §7 "불변 우선"이 깨진다 — 두 벌이 생긴 대가로 변환기가 왕복 동치성 테스트를 진다. **엔티티 추가는 스키마 변경과 함께 오는 것이 정상**이니 중복이라고 flag하지 않는다. 이 예외는 도메인↔영속에 한정된다: tool 인자·Slack 블록·뷰에 같은 논리로 계층을 늘리는 것은 여전히 지적 대상이다.
- 새 인프라(캐시, 큐, 웹서버, 검색엔진)는 코드 리뷰가 아니라 spec 논의 단계의 문제다. 코드에 먼저 나타나면 반려. DB는 changes/0028이 그 절차를 거쳐 들였으므로 **Postgres·JPA·Flyway 자체는 반려 사유가 아니다** — 다만 스키마 변경이 Flyway 마이그레이션 없이 엔티티만 고쳐 들어오면 그것은 flag 대상이다(`ddl-auto: validate`가 기동에서 깨진다).

**리뷰 질문**: 이 구조가 "1인용 로컬 앱"에서 실제로 관측된 문제를 푸는가, 아니면 관성적인 패턴 적용인가?

## 5. 패키지는 응집 단위로 작게

- 한 패키지가 **대략 10개 파일을 넘으면 분리 신호**로 본다. 숫자 자체보다 "서로 다른 관심사가 섞여 있는가"가 기준이다.
  - 좋은 선례: `slack/` → `slack/inbound/`, `slack/outbound/` 분리, `agent/` → `agent/prompt/`, `agent/tool/`, `agent/conversation/` 분리, `agent/tool/` → 검증은 `agent/tool/validation/`·턴 하네스는 `agent/turn/` 분리(ADR-64).
  - 허용 예외: 단일 관심사의 데이터 홀더 군집은 파일 수 초과를 허용한다 — `domain/`(노트 구조 record), `repository/entity/`(테이블 1:1 엔티티 — 스키마가 소유자이므로 쪼개는 기준이 따로 없다, changes/0028), `agent/tool/` 루트(툴 정의 + 인자 record — 검증은 `agent/tool/validation/`, 턴 전처리·컨텍스트 운반체는 `agent/turn/`이 담당하므로 루트에 유입시키지 않는다, ADR-64). 쪼개면 오히려 응집이 깨지는 곳이니 개수만으로 flag하지 마라.
- 분리할 때는 계층(예: `service/`, `impl/`)이 아니라 **기능·관심사**로 자른다.
- 반대로, 파일 1~2개짜리 패키지를 예비로 미리 만들지도 않는다. 분리는 커졌을 때 한다.

**리뷰 질문**: 이 패키지의 파일들이 한 문장으로 설명되는 하나의 관심사인가?

## 6. 하네스가 프롬프트보다 우선한다

LLM 관련 코드는 **프롬프트가 실패해도 시스템이 무너지지 않는 구조**인지 검사한다. 아래 위반은 발견 시 flag한다.

**§6 전체의 리뷰 질문**(아래 각 절은 이 질문을 축별로 쪼갠 것이다): 이 프롬프트 문장을 통째로 지워도 시스템이 잘못된 데이터를 저장하거나 무한 루프에 빠지지 않는가?

### 6.1 프롬프트에만 존재하는 비즈니스 규칙
프롬프트 문자열 안에만 정의된 비즈니스 규칙(값 범위, 필수 필드, 출력 형식, 개수 제한 등)은 flag. 같은 규칙이 코드 레벨 검증(record 생성자 검증, 스키마 검증, 파싱 후 assert)으로 이중 강제되면 통과. 프롬프트가 유일한 방어선이면 🔴 Important.
- 판단 시 프롬프트 템플릿과 그 출력을 소비하는 코드를 모두 확인하고, 검증 코드의 file:line을 근거로 제시하라.

### 6.2 검증 없이 신뢰되는 LLM 출력
LLM 응답을 파싱·검증 없이 그대로 사용하는 경로는 flag:
- JSON 응답을 `JsonNode`/Map으로 받아 키 존재 확인 없이 접근
- 응답 문자열을 구조 검증 없이 그대로 저장하거나 다음 프롬프트에 삽입
- 파싱 실패 시 예외를 삼키거나, spec이 정의한 실패 응답 없이 크래시로 이어짐

파싱 실패는 정상 시나리오다. 재시도, 또는 spec 정의 폴백(plan §7)으로 수렴하는 예외 전파면 통과 — §7 "실패는 숨기지 않는다"와 같은 원칙이다. 삼키거나 정의되지 않은 실패로 새면 🔴 Important.

### 6.3 프롬프트 지시로만 막고 있는 동작 제약
"~하지 마세요" 류의 프롬프트 지시는 규칙의 성격을 먼저 판정하고, 등급에 맞는 강제 수단을 요구하라.

**판정 절차** — 등급은 이름의 어감으로 고르지 말고 아래 두 질문을 순서대로 통과시켜 정한다. 두 질문 모두 규칙의 *내용*이 아니라 **위반을 판정할 근거가 어디에 있는가**를 묻는다 — 같은 성격의 규칙이라도 근거가 손 안에 있으면 강제할 수 있고 프로세스 밖에 있으면 못 한다.

- **Q1 — 위반 판정에 필요한 근거가 프로세스 안에 이미 있는가?** 툴 인자·사용자 원문·저장소 상태·루프 카운터처럼 우리 코드가 판정 시점에 들고 있는 값으로 갈리는가. 판정하려면 외부 세계를 조회해야 하거나(페이지 내용·실시간 사실) 값이 뜻하는 바를 이해해야 하면 "아니오"다. → **아니오면 3등급이다**(Q2를 묻지 않는다).
- **Q2 — 그 근거로 위반이 결정적으로 갈리는가?** 같은 입력에 같은 판정이 나오고 참·거짓이 일의적인가. → **예면 1등급.** 아니오지만 위반 대부분을 잡는 판정 규칙을 문장으로 쓸 수 있으면 **2등급**이다(오탐이 나도 거부 사유로 모델이 루프 안에서 정정하므로 근사로도 값이 있다).

등급별 요구:

1. **완전 강제 가능** (Q1 예 + Q2 예) — 스키마/코드로 결정적으로 막을 수 있는 규칙. 코드 강제 없이 프롬프트에만 있으면 🔴 Important.
   - 툴 인자 제한 → 툴 스키마의 enum/range로 강제 (판정 근거 = 인자 그 자체)
   - 루프 종료 → max iteration 카운터로 강제 (판정 근거 = 우리가 세는 카운터)
   - 툴 호출 순서 → 상태에 따른 툴 노출 제어로 강제 (판정 근거 = 우리가 들고 있는 상태)
2. **근사 검증 가능** (Q1 예 + Q2 아니오) — 완전 강제는 불가하지만 휴리스틱 게이트로 위반 대부분을 탐지할 수 있는 규칙. 게이트 없이 프롬프트에만 있으면 🟡 Minor로 flag하고 게이트 구현안을 함께 제시하라.
   - 선례: 다중 날짜 발화(V-16) — 판정 근거가 **사용자 원문**이라 손 안에 있고(Q1 예), 날짜 표기 추출이 완전하지는 않지만(Q2 아니오) "서로 다른 절대 날짜 2개 이상"이라는 판정 규칙을 쓸 수 있어 제안 tool이 사유와 함께 거부한다.
3. **의미 판단 전용** (Q1 아니오) — 자연어 이해 자체가 규칙이거나(어미 정규화, 뉘앙스 보존, 감상/피드백 분리) 판정 근거가 프로세스 밖에 있는 경우. 코드 강제를 요구하지 마라 — §6.5 격리 테스트와 §6.7 관측의 대상이다. 단 아래 면제 조건을 확인해야 판정이 끝난다.

**§6.1과의 관계 — 3등급은 §6.1의 🔴을 면제하되, 조건이 붙는다.** 이 예외 관계는 지금까지 암묵이었고 그래서 판정이 갈렸다: 같은 규칙이 §6.1로 읽으면 "프롬프트가 유일한 방어선 = 🔴"이고 §6.3-3등급으로 읽으면 통과였다(ADR-72 ②가 말하는 기준 오작동). 해소는 이렇다 — **3등급 판정은 §6.1의 🔴을 면제하지만, 아래 둘이 함께 성립할 때만이다.**

- (a) **위반이 드러나는 경로가 있다** — 그 규칙의 위반을 사후에라도 관측하는 항목(또는 프록시)이 plan §6에 있다. 없으면 위반은 영구히 보이지 않고, "관측의 대상이다"라는 면제 사유 자체가 거짓이 된다.
- (b) **위반이 사용자 확인을 통과하지 않고 저장에 도달하지 않는다** — 위반한 값이 미리보기에 드러나고 [저장] 버튼을 거친다(ADR-3·ADR-45). 이 프로젝트에서 3등급 면제가 안전한 이유가 여기다 — §6.8-③의 신뢰 경계와 **같은 방어 구조**이고 같은 전제(쓰기 경로의 협소함) 위에 서 있다. 그 전제를 넓히는 변경은 3등급 면제도 함께 흔든다.

둘 중 하나라도 없으면 3등급이라도 §6.1의 🔴로 되돌린다. 3등급은 "방어가 없어도 된다"가 아니라 **"방어를 코드 강제가 아닌 곳에서 받는다"**는 판정이고, 그 "아닌 곳"이 실재하는지까지 확인해야 한다.

판정 선례: `sources` 동일성 가드(AC-58)를 이 절차로 판정한 기록은 `ideas/harness-review-2026-07-28.md` §5에 있다 — Q1 아니오(판정 근거 = 출처 페이지의 내용, 프로세스 밖) → 3등급, 면제 조건 (a)·(b) 충족.

코드 강제가 있고 프롬프트는 보조면 통과. 등급 판정이 애매하면 낮은 등급으로 보고하고 불확실함을 명시하라 — 위 면제 조건이 있으므로 3등급으로 미끄러뜨리는 것이 무방비 통과가 되지는 않는다.

게이트·서버 검증이 LLM에 돌려주는 **거부 사유는 모델 대면 메시지다** — 무엇이 틀렸나·왜·다음 행동을 담아야 모델이 루프 안에서 자가 정정한다(plan ADR-45 POLICY). 근거 없는 bare rejection(`"invalid"`, `"rejected"` 류)은 정정 루프를 헛돌게 하므로 🟡 Minor로 flag하고 사유 문안을 함께 제시하라. 강제는 코드가, 정정은 근거가 맡는다 — 게이트는 차단기이자 근거 공급기다.

### 6.4 에이전트 루프 안전장치
에이전트 루프(툴 호출 반복 구조)에서 다음이 누락되면 flag:
- 최대 반복 횟수 제한 (누락 시 🔴 Important)
- 툴 실행 예외가 루프를 죽이지 않고 에러 결과로 모델에 전달되는 처리
- 토큰/비용 상한 또는 타임아웃

### 6.5 프롬프트 변경에 취약한 핵심 로직의 테스트 부재
프롬프트 수정만으로 동작이 바뀔 수 있는 핵심 경로(출력 파싱, 툴 인자 매핑, 분기 판단)에, LLM 호출을 §2 경계(`ChatClient` 등)에서 격리하고 파싱·검증·분기 로직만 검증하는 테스트가 없으면 flag. 테스트를 위해 새 인터페이스를 만들지 말 것(§2).

### 6.6 프롬프트와 스키마의 이중 정의
프롬프트 템플릿의 출력 형식 지시와 파싱 스키마(record/DTO)가 서로 다른 위치에 중복 정의되어 한쪽만 수정하면 어긋나는 구조는 flag. 스키마에서 형식 지시를 파생시키거나, 최소한 같은 패키지에 두고 POLICY 주석으로 연결 관계를 명시하면 통과.

### 6.7 실패 경로의 진단 가능성 (관측)

핵심 질문: **이 경로가 실패했을 때 로그만으로 원인을 재구성할 수 있는가.** 답이 "아니오"면 flag한다.

**§6.5와의 관계 — 대체가 아니라 보완이다.** §6.5는 *검증*을 본다(테스트가 이 로직을 붙잡고 있는가), 이 절은 *진단*을 본다(실사용에서 터졌을 때 원인에 도달할 수 있는가). 테스트가 있어도 관측이 없으면 테스트가 재현하지 못한 실패는 영구 미해결로 남고, 관측이 있어도 테스트가 없으면 회귀를 막지 못한다. 둘 중 하나로 다른 하나를 면제하지 마라.

**적용 범위**: 이 절만은 LLM 경로에 한정되지 않는다 — 외부 경계(LLM·Slack·DB·파일·프로세스) 어디든 같은 질문을 받는다. 이 프로젝트에서 관측은 부수 기능이 아니라 **자산**이다: plan §6이 관측 항목을 정의하고, ADR-69 ①의 "실사용 로그 → eval 케이스 박제" 루프가 그 로그를 입력으로 쓴다. 관측이 비면 그 루프가 끊긴다.

최소 다음을 확인한다.

1. **실패·폴백 지점이 사유를 구분해 남기는가.** 같은 예외 타입·같은 폴백에 도달하는 원인이 둘 이상이면 로그에서 갈려야 한다. **사용자 대면 응답은 하나로 뭉쳐도 된다**(1인용 앱에서 사용자가 할 수 있는 일은 대개 "다시 말한다" 하나다) — 하지만 로그가 함께 뭉치면 대응을 고를 수 없다. 상한·게이트가 여럿이면 **어느 상한에 걸렸는지**가 남아야 조정 대상이 판별된다.
2. **비용·성능의 지배 변수가 관측되는가.** 그 경로의 비용·소요를 결정하는 값이 로그에 있는가. 결과 총량만 남기고 그것을 움직이는 변수를 빼면, 숫자가 변했을 때 원인을 못 가른다 — "관측을 축적한 뒤 기본값을 조정한다"는 계획(ADR-62)은 **원인을 가를 수 있는** 축적을 전제한다.
3. **외부 경계 호출의 결과 크기·소요가 남는가.** 응답의 크기(문자·바이트·건수)와 소요 시간. 특히 결과가 다시 모델 컨텍스트로 되먹여지는 경로(tool 결과 등)는 **크기가 곧 다음 요청의 비용**이고, 증상은 원인과 무관해 보이는 상한 도달로만 드러난다. 여기서 요구하는 것은 제한 도입이 아니라 **판단 근거의 확보**다 — right-sizing(루트 `CLAUDE.md` §4)이 "관측된 뒤 도입"인데 크기를 안 남기면 그 관측 자체가 불가능하다.
4. **예외 로깅에 무엇이 실패했는지의 맥락이 함께 남는가.** 스택 트레이스만으로는 어느 입력·어느 이벤트에서 터졌는지 못 찾는다. 예외를 잡아 로그만 남기고 진행하는 지점(백그라운드 워커·핸들러)은 특히 flag 대상이다 — §7 "실패는 숨기지 않는다"의 관측 쪽 짝이다. 원문·개인 데이터를 로그에 남기는 것 자체는 허용된다(NFR-7 — 마스킹은 spec 비범위, `logs/`를 개인 데이터로 취급하는 것이 그 대가다).
5. **한 처리 단위의 로그가 상관 키로 이어지는가.** 하나의 턴·이벤트가 여러 지점에 로그를 남기면 공통 키 없이는 시간순 추정으로 재구성해야 한다. 로그 지점이 늘어나는 변경에서 flag.

**severity**: 기본 🟡 Minor다 — 관측 부재는 그 자체로 잘못된 저장·무한 루프·비용 폭주를 만들지 않고, **원인 규명을 막는다**(2026-07-28 하네스 리뷰가 관측 finding 4건을 전부 🟡로 판정한 것과 같은 기준). 상한·검증 **자체**가 없으면 그것은 이 절이 아니라 §6.1·§6.2·§6.4의 🔴 대상이다 — 관측으로 상한을 대신하려는 제안은 이 절이 아니라 그 절로 판정하라.

**리뷰 질문**: 이 경로가 실사용에서 실패했을 때, `logs/`만 보고 **무엇이 왜 실패했는지**와 **무엇을 조정해야 하는지**를 말할 수 있는가?

### 6.8 모델 컨텍스트에 들어가는 것 (입력)

핵심 질문: **이번 요청에 무엇이 얼마나 들어가는지 말할 수 있는가.**

§6.1~6.6은 전부 모델 *출력* 쪽을 본다 — 프롬프트 규칙, 출력 검증, 동작 제약, 루프 안전, 테스트, 스키마. 모델에 **무엇을 넣는지**는 그 반대편이고, 비용·상한 도달·오도 가능성이 전부 여기서 결정된다. 컨텍스트를 늘리거나 새 소스를 들이는 변경은 아래 셋을 받는다.

1. **컨텍스트에 들어가는 것의 크기를 무엇이 결정하는가.** 사용자 데이터·저장소 내용·외부 호출 결과가 그대로 컨텍스트로 흘러드는 경로에서, 그 양을 키우는 변수가 무엇인지(건수인가·원문 길이인가·누적인가) 리뷰에서 답할 수 있어야 한다. **여기서 요구하는 것은 상한 도입이 아니다** — 실제 압박이 관측되기 전의 페이지네이션·요약 선제 도입은 right-sizing(루트 `CLAUDE.md` §4) 위반이다. 요구는 두 가지다: (a) 그 변수를 답할 수 있을 것, (b) 방어선이 **사후 판정 상한**(누적 토큰 등)뿐이면 그 사실이 인지될 것 — 사후 판정은 비용을 지불한 뒤에 걸리고, 증상은 원인과 무관해 보이는 상한 도달로만 드러난다(§6.7-③의 입력 쪽 짝).
2. **무엇이 들어가는지가 한 곳에서 읽히는가.** 컨텍스트 조립 지점이 흩어지면 "이 턴에 무엇이 들어갔나"에 답하려고 코드 전체를 훑어야 한다. 프롬프트 조립을 담당하는 지점 **밖에서** 컨텍스트에 문자열을 덧붙이는 경로는 flag.
3. **외부 유래 텍스트가 들어오는 경로에 무엇이 방어선인가 (신뢰 경계).** 사용자 발화가 아닌 출처(웹 검색 결과·OCR·외부 파일)의 텍스트가 컨텍스트에 들어오면 그것은 **모델에 대한 지시로 읽힐 수 있다.** 이 프로젝트에서 그 위험이 낮은 이유는 내용 필터링이 아니라 **쓰기 경로의 협소함**이다 — 에이전트가 데이터를 바꾸는 경로는 제안 tool 2종뿐이고 그 효과는 pending 생성·갱신까지이며, 노트 커밋은 [저장] 버튼의 결정론 경로뿐이다(plan ADR-45·ADR-3). 즉 외부 텍스트가 모델을 오도해도 **사용자 눈을 통과하지 않으면 아무것도 저장되지 않는다.** 이 방어는 그 전제 위에서만 성립한다 — **쓰기 경로를 넓히는 변경**(새 쓰기 tool, 사용자 확인 없는 커밋, 자동 확정)은 외부 유래 텍스트의 유입 경로와 **함께** 평가해야 하고, 전제를 건드리면서 그 사실을 인지하지 않은 변경은 🔴 Important다.

**severity**: 1·2는 기본 🟡 Minor다(판단 근거·가독성의 문제이지 그 자체로 오염을 만들지 않는다). 3은 위 조건(쓰기 경로 확대)에서 🔴 Important — 외부 텍스트가 사용자 확인 없이 저장에 도달할 수 있으면 "잘못된 저장"이다.

**리뷰 질문**: 이 변경이 모델 요청에 무엇을 더 넣는가, 그 양은 무엇이 키우는가? 새로 들어오는 텍스트가 우리 코드가 만든 것이 아니라면, 그것이 모델을 오도했을 때 무엇이 막는가?

### 6.9 프롬프트 규칙 밀도

핵심 질문: **규칙을 하나 더 넣을 때, 기존 규칙과 간섭하지 않음을 무엇으로 확인하는가.**

이 절은 **문구가 아니라 밀도를 본다.** 표현·어조 개선은 계속 스킵 대상이지만("스킵할 것"), 규칙의 **수와 상호작용**은 리뷰 대상이다 — 이 프로젝트에는 그것이 문체가 아니라 직접적인 비용 요인이라는 실증이 있다: `mocha.agent.model`은 경량 모델로 시작했다가 **다중 날짜 규칙의 반복 위반으로 상향**됐고 지금도 "분해 안정화 후 다운그레이드 실험 예정" 상태다(plan §5 — 0021 TΔ3b). 규칙 하나를 더 얹는 비용은 그 문장의 토큰이 아니라, **나머지 규칙과 함께 지켜지게 하는 데 필요한 모델 등급**이다.

확인할 것.

1. **새 규칙이 기존 규칙과 충돌·중복하지 않는가.** 같은 대상에 대한 지시가 두 절에 나뉘면 모델이 어느 쪽을 따를지는 설계가 아니라 우연이다. 기존 절에 편입할 수 있는 규칙을 새 절로 늘리는 변경은 flag.
2. **규칙 추가가 행동 회귀 측정을 동반하는가.** 시스템 프롬프트는 행동 레이어이므로 `evalTest` 전/후 기록이 필수다(plan ADR-69 ②) — 간섭 여부는 논증이 아니라 숫자로 확인한다. **이 절의 실효 강제 수단은 이것 하나뿐이다.** 측정 없는 행동 레이어 변경은 ADR-69 POLICY 위반이므로 severity와 무관하게 그 델타의 완료 조건을 못 채운 것으로 본다.
3. **코드로 강제 가능한 규칙을 프롬프트에 쌓고 있지 않은가.** §6.1·§6.3을 반대 방향에서 묻는 질문이다 — 강제 수단이 있는 규칙을 프롬프트 문장으로 해결하면 규칙 수만 늘고 방어선은 안 는다. 규칙 밀도의 가장 흔한 증가 원인이다.

**severity**: 기본 🟡 Minor. 3에 해당하면 §6.1·§6.3의 등급 판정으로 넘겨 판정한다(이 절이 그 🔴을 완화하지 않는다).

**리뷰 질문**: 이 규칙이 없으면 무엇이 깨지는가? 그것을 코드가 아니라 문장으로 막는 이유가 있는가? 늘어난 규칙이 기존 규칙을 흔들지 않는다는 근거가 숫자로 있는가?

## 7. 그 외 일반 원칙

위 항목에 걸리지 않는 부분은 일반적인 객체지향 설계 원칙을 따른다. 이 프로젝트에서 특히 자주 보는 것:

- **단일 책임**: 클래스가 "~하고 ~도 한다"로 설명되면 분리를 검토한다. 단, §2·§4에 어긋나는 분리(추상화 계층 추가)로 풀지 말고 클래스 분할로 푼다.
- **불변 우선**: 데이터 홀더는 record, 컬렉션은 방어적 복사 또는 불변 컬렉션.
- **생성자 주입**: 의존은 생성자로 받는다. 필드 주입·세터 주입 금지.
- **실패는 숨기지 않는다**: 예외를 삼키고 빈 값을 돌려주는 코드 금지. 복구할 수 없으면 전파하고, 사용자에게 알려야 하면 spec의 오류 처리 흐름을 따른다.
- **테스트 용이성은 §2 경계로 확보한다**: 테스트를 위해 새 인터페이스를 만들지 말고, 이미 있는 경계(저장·LLM·Slack)에서 격리한다.

---

## 검증 기준

- 동작에 대한 주장은 file:line 근거를 제시하라. 이름이나 구조에서 추론한 내용은 finding으로 올리지 마라.
- "프롬프트에만 있다"(§6.1)고 주장하려면 실제로 코드 레벨 검증을 탐색한 뒤 없음을 확인하고 보고하라.
- 확신이 없는 finding은 severity를 낮추고 불확실함을 명시하라.

## 스킵할 것

- 프롬프트 문구 자체의 표현·어조 개선 제안 (§6 위반이 아닌 한) — 스킵하는 것은 **"같은 규칙을 더 나은 문장으로"**다. **규칙의 수·간섭은 문체가 아니므로 스킵 대상이 아니다**(§6.9): "규칙이 하나 늘었는데 기존 규칙과 간섭하는가", "이 규칙은 코드로 강제할 수 있지 않은가"는 리뷰한다.
- 생성 코드, 빌드 산출물
- 린터·컴파일러가 이미 잡는 스타일 문제

## 이 문서의 갱신

이 기준은 고정 체크리스트가 아니라 **갱신되는 자산**이다 — 제정 시점의 시야가 영구화되면 놓친 축을 다음 리뷰도 그대로 놓친다. 트리거의 정의·근거·기각 대안은 `specs/coffee-note-agent/plan.md#ADR-72`가 **단일 소유**하고, 여기서는 리뷰 중에 밟게 되는 절차만 적는다.

- **finding이 이 문서의 어느 항목으로도 걸리지 않으면**, 그 문제를 고치는 것과 별개로 기준에 항목을 추가할지 판단하고 사유를 남긴다 — "추가 안 함"도 결론이지만 무기록 통과는 안 된다(ADR-72 ①).
- **항목끼리 상충하거나 등급 판정이 리뷰어마다 갈리면**, 한쪽을 골라 보고하는 데서 멈추지 말고 그 판정 기준을 구체화한다(ADR-72 ②). §6.3의 판정 절차와 §6.1 면제 조건이 이 트리거의 첫 산출물이다.
- **델타 완료 시** 한 번도 적용되지 않은 항목이 반복되면 그 항목의 유효성을 재검토한다 — 과잉 기준도 부채다(ADR-72 ③).
- **수정은 델타로 한다.** 이 문서를 델타 없이 직접 고치지 않는다 — 기준 변경 자체가 리뷰 대상이다.
- **개별 사례는 여기 쌓지 않는다.** 이 문서는 판정 절차를 담고, 재판정 결과·판정 선례는 그 리뷰의 기록 문서가 보관한다(선례: `ideas/harness-review-2026-07-28.md` §3~§5). 사례가 기준 문서로 흘러들면 판별 항목과 예시가 뒤섞여 항목 수만 늘어난다.

---

## 리뷰 체크리스트 (요약)

- [ ] 구현체 이름이 의존 대상을 드러내는가 (`Impl`/`Default` 없음)
- [ ] 표준·프레임워크(Spring AI 등)에 대응되는 개념은 그 용어를 따르는가 (불필요한 자체 조어 없음)
- [ ] 새 인터페이스가 §2 허용 경계 표 안에 있는가 (구현체 안쪽의 Spring Data 조각은 대상 아님)
- [ ] 사용처 없는 코드·파라미터·주석 코드가 없는가
- [ ] 계층 패키지(port/adapter/usecase 류)·중복 DTO 계층이 없는가 (JPA 엔티티·`NoteEntityMapper`는 §4의 확정 예외)
- [ ] 스키마 변경이 Flyway 마이그레이션으로 들어왔는가 — 엔티티만 고쳐 `ddl-auto: validate`를 깨뜨리지 않는가 (§4)
- [ ] 패키지가 하나의 관심사로 설명되는가 (10파일 초과 시 분리 검토)
- [ ] 강제·검증 가능한 비즈니스 규칙이 프롬프트에만 있지 않은가 (§6.1, §6.3 판정 절차 Q1·Q2 적용)
- [ ] 3등급(의미 판단 전용)으로 판정한 규칙에 면제 조건 (a) 관측 항목과 (b) 사용자 확인 게이트가 **둘 다** 있는가 (§6.3)
- [ ] 게이트·검증의 거부 사유가 무엇·왜·다음 행동을 담는가 — bare rejection 없음 (§6.3)
- [ ] LLM 출력이 검증·파싱 실패 처리를 거치는가 (§6.2)
- [ ] 에이전트 루프에 max iteration·예외 처리·상한이 있는가 (§6.4)
- [ ] 파싱·분기 로직에 LLM 격리 테스트가 있는가 (§6.5)
- [ ] 실패·폴백 지점이 사유를 구분해 남기는가 — 같은 폴백에 원인이 여럿이면 로그에서 갈리는가 (§6.7)
- [ ] 비용·성능의 지배 변수와 외부 경계 호출의 결과 크기·소요가 관측되는가 (§6.7)
- [ ] 예외 로깅에 무엇이 실패했는지의 맥락이 있고, 흩어진 로그가 상관 키로 이어지는가 (§6.7)
- [ ] 모델 컨텍스트에 들어가는 것의 크기를 무엇이 키우는지 답할 수 있고, 조립 지점이 한 곳인가 (§6.8)
- [ ] 외부 유래 텍스트(검색 결과·OCR)가 들어오는 경로에서 쓰기 경로의 협소함(제안 tool 2종 + 버튼 커밋)이 유지되는가 (§6.8)
- [ ] 프롬프트 규칙 추가가 기존 규칙과 충돌·중복하지 않고, 코드로 강제 가능한 규칙을 문장으로 대신하고 있지 않은가 (§6.9)
- [ ] 행동 레이어(프롬프트·모델·tool 스키마·검증기·세그먼터) 변경 시 evalTest 전/후 결과가 델타에 기록됐는가 (plan ADR-69, §6.9-2)
- [ ] 생성자 주입·불변·예외 비은폐를 지켰는가
- [ ] 이 리뷰의 finding이 위 항목 어디에도 안 걸린 것이 있으면, 기준 갱신 여부를 판단하고 사유를 남겼는가 (§이 문서의 갱신, ADR-72 ①)
- [ ] (공통) spec 범위 안의 변경인가, `.env`·`data/`·`logs/`·**DB 덤프**가 새어 들어가지 않았는가 (덤프는 `.gitignore`가 잡아주지 않는다)